<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:id="@+id/tv_home_progress_title"
        android:text="@string/Home_progress_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <ProgressBar
        android:layout_marginTop="@dimen/home_progress_margin_top"
        android:id="@+id/pb_home_fetch_config"
        android:layout_width="@dimen/home_progress_size"
        android:layout_height="55dp"
        app:layout_constraintLeft_toLeftOf="@+id/tv_home_progress_title"
        app:layout_constraintRight_toRightOf="@+id/tv_home_progress_title"
        app:layout_constraintTop_toBottomOf="@+id/tv_home_progress_title">
    </ProgressBar>
</android.support.constraint.ConstraintLayout>
